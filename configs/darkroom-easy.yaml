# Darkroom (10x10, horizon 100) — sparse-reward 2D goal search.
env_name: darkroom-easy
dataset_size: 10000
n_envs: 1000
batch_size: 64
num_epochs: 100
eval_episodes: 40
dagger_steps: 5    # ASTEROID: on-policy iterations (context curriculum)
n_episodes: 5      # DPT: episodes of random context per trajectory
